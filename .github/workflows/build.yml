name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Unzip Vibrax
      run: unzip "Vibrax.zip" -d project

    - name: Enter Vibrax folder
      run: |
        cd project/Vibrax
        ls -la

    - name: Install CocoaPods
      run: |
        cd project/Vibrax
        sudo gem install cocoapods
        pod install

    - name: Build archive
      run: |
        cd project/Vibrax
        xcodebuild \
          -workspace Vibra.xcworkspace \
          -scheme Vibra \
          -configuration Release \
          -archivePath build/Vibra.xcarchive \
          archive
